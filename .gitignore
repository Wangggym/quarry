# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/

# Test coverage artifacts
.coverage
.coverage.*
htmlcov/

# Quarry — never commit a workspace with real connection secrets
connections.toml
workspace/
*.local.toml
# ...but the credential-free example workspace ships with the repo
!examples/workspace/
!examples/workspace/connections.toml

# Frontend (Node is dev-only; built assets ship in the wheel)
web/node_modules/
web/tsconfig.tsbuildinfo

# OS / editor
.DS_Store
.idea/
.vscode/
